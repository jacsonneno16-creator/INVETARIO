CORREÇÕES APLICADAS

1. Login não é mais automático.
2. Sessão do Firebase Auth usa persistência NONE.
3. Campos de login não usam preenchimento automático do navegador.
4. Regras adicionadas para:
   - dt_inventarios/{id}/base_chunks
   - dt_locais_chunks
   - dt_enderecos

IMPORTANTE: após enviar os arquivos, publique também as regras:

firebase deploy --only firestore:rules,hosting
