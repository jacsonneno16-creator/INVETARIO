# Correção v294

- Corrigida classe dos modais de cadastro: `modal-bg open`.
- Compatibilidade CSS para `open` e `on`.
- Atualizado cache do Service Worker para obrigar carregamento dos arquivos novos.
- Incluídos `52-cadastros-v287.js` e `analista-cadastros-v287.css` no pré-cache.
- Validada sintaxe JavaScript.
