CORREÇÃO APLICADA

1. O login anterior não é mais reaproveitado.
2. O painel lê endereços de dt_locais_chunks, com fallback em dt_locais.
3. As regras permitem dt_locais_chunks e dt_inventarios/{id}/base_chunks.
4. firebase.json agora publica as regras do Firestore.

PUBLICAÇÃO OBRIGATÓRIA:
firebase deploy --only hosting,firestore:rules

Após publicar, abra o site com Ctrl+F5 ou limpe os dados do site.
